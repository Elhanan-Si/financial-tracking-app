# תוכנית עבודה ופירוק משימות לפיתוח (WBS - Work Breakdown Structure)
## אפליקציית מעקב פיננסי אישי (Personal Financial Tracking App)
**מבוסס על מסמך אפיון מערכת (SDD v1.0) | טכנולוגיה: Flutter (Offline-First, Clean Architecture)**

---

## 📑 תוכן עניינים
1. [סקירה כללית ועקרונות פיתוח](#1-סקירה-כללית-ועקרונות-פיתוח)
2. [מפת ארכיטקטורה ורכיבי תשתית](#2-מפת-ארכיטקטורה-ורכיבי-תשתית)
3. [מפת דרכים ושלבי פיתוח (Milestones & Phases)](#3-מפת-דרכים-ושלבי-פיתוח-milestones--phases)
4. [פירוט משימות לפי פאזות (Detailed Task Specifications)](#4-פירוט-משימות-לפי-פאזות-detailed-task-specifications)
   - [Phase 1: תשתיות ליבה, בסיס נתונים, אבטחה ועיצוב אדפטיבי](#phase-1-תשתיות-ליבה-בסיס-נתונים-אבטחה-ועיצוב-אדפטיבי)
   - [Phase 2: ניהול חשבונות, קטגוריות ועסקאות יומיומיות](#phase-2-ניהול-חשבונות-קטגוריות-ועסקאות-יומיומיות)
   - [Phase 3: מנוע ייבוא חכם, זיהוי כפילויות וסיווג אוטומטי](#phase-3-מנוע-ייבוא-חכם-זיהוי-כפילויות-וסיווג-אוטומטי)
   - [Phase 4: תקציב, ניצול תקציב, חיובי אשראי ותזרים מזומנים](#phase-4-תקציב-ניצול-תקציב-חיובי-אשראי-ותזרים-מזומנים)
   - [Phase 5: השקעות, נכסים פנסיוניים, התחייבויות וסנכרון שערים](#phase-5-השקעות-נכסים-פנסיוניים-התחייבויות-וסנכרון-שערים)
   - [Phase 6: דשבורדים, שווי נקי, תובנות וניתוחים פיננסיים](#phase-6-דשבורדים-שווי-נקי-תובנות-וניתוחים-פיננסיים)
   - [Phase 7: גיבוי, שחזור, אופטימיזציה, בדיקות והשקה](#phase-7-גיבוי-שחזור-אופטימיזציה-בדיקות-והשקה)
5. [מטריצת מעקב מסכים מול משימות (Screen Coverage Matrix)](#5-מטריצת-מעקב-מסכים-מול-משימות-screen-coverage-matrix)
6. [מטריצת דרישות לא-פונקציונליות ואימותן](#6-מטריצת-דרישות-לא-פונקציונליות-ואימותן)

---

## 1. סקירה כללית ועקרונות פיתוח

מסמך זה מפרק את כלל הדרישות שהוגדרו במסמך האפיון (`SDD_Financial_tracking.md`) ל-33 משימות פיתוח מוגדרות ומפורטות, הניתנות להמרה ישירה ל-Tickets במערכות ניהול משימות (כגון Jira, GitHub Projects וכדומה).

### עקרונות ברזל לפיתוח:
1. **Offline-First**: כל הלוגיקה, השמירה, החישובים והסיווגים מתבצעים מקומית במכשיר ללא תלות בשרת backend.
2. **ארכיטקטורה נקייה (Clean Architecture)**:
   - **Presentation Layer**: מסכי ורכיבי Flutter, מצב מנוהל באמצעות `Riverpod`.
   - **Domain Layer**: ישויות טהורות (`Entities`) ו-`Use Cases` בלתי תלויים ב-UI.
   - **Data Layer**: יישום `Repository Pattern`, גישה ל-SQLite מוצפן באמצעות `Drift` ו-`SQLCipher`.
3. **עיצוב וממשק משתמש (UI/UX)**:
   - עברית מלאה בלבד (RTL מלא בכל רכיבי המערכת, כולל גרפים וטבלאות).
   - ללא אימוג'ים בשום מקום באפליקציה – אינדיקציות מבוססות אייקונים אחידים וצבע בלבד.
   - ערכת נושא בהירה בלבד (Light Theme) עם היררכיה חזותית ברורה.
   - רספונסיביות אדפטיבית לטלפון, טאבלט ודסקטופ (Material 3 Adaptive Breakpoints).
4. **פרטיות ואבטחה**: הצפנת מסד נתונים במנוחה (`SQLCipher`), שמירת מפתחות ב-`Keystore/Keychain`, אימות ביומטרי ונעילה אוטומטית.

---

## 2. מפת ארכיטקטורה ורכיבי תשתית

```
lib/
├── app/                      # הגדרות אפליקציה, ראוטינג (GoRouter), Theme, Localization
├── core/                     # תשתיות משותפות
│   ├── constants/            # צבעים, טיפוגרפיה, מרווחים, שגיאות
│   ├── database/             # חיבור Drift, SQLCipher, Migrations, DAOs
│   ├── errors/               # Exceptions & Failures
│   ├── security/             # Keystore, Biometrics, Auto-Lock Service
│   ├── services/             # Sync Service, File I/O, Backup Service
│   └── utils/                # עיצוב מספרים/מטבעות/תאריכים עבריים, רספונסיביות
└── features/                 # מודולים לפי תחומי עניין (Feature-Driven)
    ├── auth_lock/            # נעילה, ביומטרי, קוד גיבוי
    ├── accounts/             # חשבונות, אמצעי תשלום, ארנקים
    ├── categories_tags/      # קטגוריות, תגיות, בתי עסק
    ├── transactions/         # הזנה מהירה, תשלומים, הוראות קבע, העברות, פיצולים, רשימה
    ├── import_export/        # ייבוא קבצים (ישראכרט/לאומי/פאג"י/OneZero), מיפוי, איחוד
    ├── auto_categorization/  # מנוע הצעות סיווג ולמידה מקומית
    ├── budgets/              # תקציב חודשי, גלגול יתרות, התרעות ניצול
    ├── cash_flow/            # תחזית תזרים, חיזוי חיובי אשראי
    ├── investments/          # תיק מניות, Finnhub API, מדדי ייחוס, רב-מטבעי
    ├── pension_assets/       # פנסיה, גמל, השתלמות
    ├── non_market_assets/    # נדל"ן, הלוואות, משכנתאות, לוחות סילוקין
    ├── net_worth/            # שווי נקי, תמונות מצב (Snapshots)
    ├── insights_analytics/   # דשבורד ראשי, פילוח הוצאות, ניתוח מגמות, שיעור חיסכון
    └── backup_settings/      # גיבוי, שחזור, הגדרות כלליות
```

---

## 3. מפת דרכים ושלבי פיתוח (Milestones & Phases)

| פאזה | תחום מרכזי | משימות כלולות | שבועות משוערים | תוצר מרכזי (Deliverable) |
|---|---|---|---|---|
| **Phase 1** | תשתיות, מסד נתונים, אבטחה ועיצוב אדפטיבי | TASK-01 עד TASK-04 | 1 - 2 | תשתית פרויקט יציבה, DB מוצפן, ביומטרי, מערכת ניווט רספונסיבית |
| **Phase 2** | חשבונות, קטגוריות ותנועות יומיומיות | TASK-05 עד TASK-12 | 3 - 5 | הזנה ועריכת תנועות, תשלומים, הוראות קבע, העברות וחיפוש מתקדם |
| **Phase 3** | מנוע ייבוא חכם, זיהוי כפילויות וסיווג אוטומטי | TASK-13 עד TASK-15 | 6 - 7 | ייבוא קובצי אקסל/CSV מבנקים ישראליים, מניעת כפילויות ומנוע למידה מקומי |
| **Phase 4** | תקציב, חיובי אשראי ותזרים מזומנים | TASK-16 עד TASK-20 | 8 - 9 | ניהול תקציב עם גלגול יתרות, ריכוז חיובי אשראי ותחזית תזרים 30/60/90 יום |
| **Phase 5** | השקעות, פנסיה, התחייבויות וסנכרון שערים | TASK-21 עד TASK-25 | 10 - 12 | תיק מניות מסונכרן (Finnhub API), נכסים פנסיוניים, לוח סילוקין ורב-מטבעי |
| **Phase 6** | דשבורדים, שווי נקי ותובנות פיננסיות | TASK-26 עד TASK-30 | 13 - 14 | דשבורד שווי נקי, פילוח צרכים/מותרות, ניתוח השוואתי ודשבורד ראשי |
| **Phase 7** | גיבוי, שחזור, אופטימיזציה, בדיקות והשקה | TASK-31 עד TASK-33 | 15 - 16 | גיבוי ושחזור מוצפן, עמידה בדרישות ביצועים 10k רשומות, גרסת Release |

---

## 4. פירוט משימות לפי פאזות (Detailed Task Specifications)

---

### Phase 1: תשתיות ליבה, בסיס נתונים, אבטחה ועיצוב אדפטיבי

---

#### 📌 TASK-01: הקמת תשתית הפרויקט, חבילות, RTL ו-Design System בהירה
- **עדיפות**: קריטית (Blocker)
- **מורכבות**: בינונית
- **ספרינט**: Sprint 1
- **שכבות**: `app/`, `core/constants/`, `core/utils/`
- **תיאור המשימה**:
  - עדכון `pubspec.yaml` עם כלל החבילות הנדרשות: `flutter_riverpod`, `riverpod_annotation`, `drift`, `sqlite3_flutter_libs`, `sqlcipher_flutter_libs`, `path_provider`, `local_auth`, `flutter_secure_storage`, `go_router`, `intl`, `fl_chart`, `excel`, `csv`, `crypto`, `http`.
  - הגדרת `ThemeData` בהיר בלבד (Light Theme) בהתאם למפרט: צבעי Primary, Secondary, Error, Success, Neutral מוגדרים היטב.
  - הגדרת תמיכה מלאה ב-RTL ובשפה העברית (`he_IL`) כברירת מחדל יחידה.
  - יצירת ספריית טיפוגרפיה, מרווחים (Spacing), וספריית אייקונים אחידה (Material Symbols / Cupertino Icons ללא שימוש באימוג'ים).
- **תנאים לסיום המשימה (Definition of Done)**:
  1. `flutter pub get` עובר ללא שגיאות או קונפליקטים בגרסאות.
  2. האפליקציה נפתחת במצב RTL מלא, הפונט העברי נטען בצורה חלקה וקריאה.
  3. כלל משתני הצבעים, הטיפוגרפיה והאייקונים מרוכזים ב-Design Tokens נגישים.
  4. בדיקות לינטר (`flutter analyze`) עוברות עם 0 אזהרות ושגיאות.

---

#### 📌 TASK-02: שכבת אבטחה, אחסון מאובטח, אימות ביומטרי ונעילה אוטומטית
- **עדיפות**: קריטית
- **מורכבות**: גבוהה
- **ספרינט**: Sprint 1
- **שכבות**: `core/security/`, `features/auth_lock/`
- **ישויות קשורות**: `AppSettings`, `AuthSession`
- **מסכים רלוונטיים**: מסך 21 (הגדרות אבטחה), מסך 23 (מסך כניסה ונעילה)
- **סעיף ב-SDD**: 2.5, 4.25, 6.5
- **תיאור המשימה**:
  - מימוש שירות `SecureStorageService` באמצעות `flutter_secure_storage` לניהול מפתח ההצפנה של מסד הנתונים וקוד ה-PIN של המשתמש (Keystore באנדרואיד, Keychain).
  - מימוש שירות `BiometricAuthService` באמצעות `local_auth` לזיהוי טביעת אצבע וזיהוי פנים.
  - מימוש מנגנון נעילה אוטומטית (`InactivityTimerService` / `WidgetsBindingObserver`) הנועל את האפליקציה לאחר פרק זמן מוגדר של חוסר פעילות או מעבר לרקע.
  - מימוש הסתרת תוכן רגיש בתצוגת מסך האפליקציות האחרונות (App Switcher Privacy Shield).
  - בניית מסך נעילה/כניסה (מסך 23) ומסך הגדרות אבטחה (מסך 21).
- **תנאים לסיום המשימה (Definition of Done)**:
  1. כניסה באמצעות ביומטרי פועלת במכשיר תומך ומאפשרת מעבר ישיר.
  2. במקרה של כשל ביומטרי, מופיעה אפשרות להזנת קוד PIN גיבוי.
  3. מעבר לרקע למשך הזמן המוגדר נועל את האפליקציה ומציג את מסך הנעילה בחזרה.
  4. מפתח ההצפנה נשמר בצורה מוצפנת ומאובטחת ואינו חשוף בקבצים פשוטים.
  5. בדיקות יחידה מקיפות ל-`AuthSessionNotifier` וללוגיקת הנעילה.

---

#### 📌 TASK-03: סכמת מסד נתונים מקומי מוצפן (Drift + SQLCipher) ו-DAOs
- **עדיפות**: קריטית (Blocker)
- **מורכבות**: גבוהה
- **ספרינט**: Sprint 1
- **שכבות**: `core/database/`, `data/models/`
- **ישויות קשורות**: כל 16 הישויות המרכזיות מטבלה פרק 3 ב-SDD
- **סעיף ב-SDD**: 2.2, 3, 4.25
- **תיאור המשימה**:
  - הקמת תשתית מסד נתונים מקומי באמצעות `Drift` מעל `SQLCipher` עם פתיחת חיבור מוצפן באמצעות המפתח מ-`flutter_secure_storage`.
  - הגדרת כלל הטבלאות:
    - `AccountsTable`, `TransactionsTable`, `TransactionSplitsTable`, `InstallmentPlansTable`, `InstallmentItemsTable`
    - `RecurringRulesTable`, `TransferLinksTable`, `CategoriesTable`, `TagsTable`, `MerchantsTable`
    - `BudgetsTable`, `BudgetPeriodsTable`, `ImportBatchesTable`, `ImportMappingsTable`
    - `SecuritiesTable`, `HoldingsTable`, `InvestmentTransactionsTable`, `PensionAssetsTable`, `PensionSnapshotsTable`
    - `AssetsTable`, `LiabilitiesTable`, `LoanSchedulesTable`, `ExchangeRatesTable`, `PriceQuotesTable`, `NetWorthSnapshotsTable`, `AppSettingsTable`.
  - הגדרת יחסי גומלין (Foreign Keys, Cascades, Indexes) לביצועים מהירים בשאילתות מעל 10,000 רשומות.
  - כתיבת מנגנון מיגרציות (`MigrationStrategy`) וטעינת נתוני ברירת מחדל (קטגוריות בסיס, מטבע שקל כברירת מחדל).
- **תנאים לסיום המשימה (Definition of Done)**:
  1. קובץ ה-DB נוצר בדיסק כשהוא מוצפן במלואו (פתיחה עם SQLiteViewer רגיל נכשלת ללא מפתח).
  2. פקודת `dart run build_runner build` מייצרת את כל ה-DAOs והמחלקות בצורה נקייה.
  3. נתוני Seed ראשוניים (קטגוריות הוצאה והכנסה בסיסיות בעברית) נטענים בהפעלה ראשונה.
  4. בדיקת ביצועים ראשונית: הזרקת 10,000 רשומות דמה ושאילתת שליפה בפחות מ-50ms.

---

#### 📌 TASK-04: מערכת ניווט אדפטיבית, שלד מסכים ותמיכה רספונסיבית (Mobile & Desktop)
- **עדיפות**: גבוהה
- **מורכבות**: בינונית-גבוהה
- **ספרינט**: Sprint 1-2
- **שכבות**: `app/router/`, `presentation/widgets/adaptive/`
- **מסכים רלוונטיים**: שלד האפליקציה לכל 23 המסכים
- **סעיף ב-SDD**: 2.4, 4.29, 6.6
- **תיאור המשימה**:
  - הגדרת מערכת ניתוב מבוססת `GoRouter` עם `StatefulShellRoute` לתמיכה בניווט רציף.
  - מימוש רכיב שלד אדפטיבי (`AdaptiveScaffold`):
    - **טלפון (רוחב < 600dp)**: ניווט תחתון (`BottomNavigationBar`), כפתור הזנה מהירה צף (`FAB`).
    - **טאבלט / דסקטופ (600dp - 1024dp ומעלה)**: ניווט צדי ימני קבוע (`NavigationRail` / `Sidebar` מותאם RTL), תמיכה במצב `Master-Detail` לשתי עמודות.
  - תמיכה ב-Hover states וקיצורי מקלדת בסיסיים בסביבת Desktop (Windows).
- **תנאים לסיום המשימה (Definition of Done)**:
  1. שינוי גודל החלון בזמן אמת עובר בצורה חלקה בין תפריט תחתון לתפריט צדי ללא איבוד מצב המסך.
  2. ממשק הניווט יושב בצד ימין (RTL מלא) בדסקטופ ובטאבלט.
  3. הניווט בין חמשת האזורים המרכזיים (דשבורד, תנועות, תקציב, השקעות, הגדרות) פועל בצורה תקינה.

---

### Phase 2: ניהול חשבונות, קטגוריות ועסקאות יומיומיות

---

#### 📌 TASK-05: ניהול עץ קטגוריות, תגיות ובתי עסק
- **עדיפות**: גבוהה
- **מורכבות**: בינונית
- **ספרינט**: Sprint 2
- **שכבות**: `features/categories_tags/` (Data, Domain, Presentation)
- **ישויות קשורות**: `Category`, `Tag`, `Merchant`
- **מסכים רלוונטיים**: מסך 20 (הגדרות עץ קטגוריות, תגיות ובתי עסק)
- **סעיף ב-SDD**: 4.28
- **תיאור המשימה**:
  - יצירת ה-Use Cases וה-Repository לניהול קטגוריות דו-רמתי (קטגוריית אב ותת-קטגוריה).
  - הגדרת מאפייני קטגוריה: סוג (הוצאה/הכנסה), סיווג צורך/מותרות (Needs/Wants), קבוע/משתנה (Fixed/Variable), צבע ואייקון.
  - מימוש מנגנון מיזוג קטגוריות כפולות או בתי עסק כפולים והעברה אוטומטית של כלל התנועות ההיסטוריות.
  - מסך ניהול עץ קטגוריות עם עריכה, הוספה, מחיקה (עם אזהרה ובחירת קטגוריה חלופית לתנועות קיימות).
  - ניהול רשימת תגיות חופשיות ורשימת בתי עסק עם שיוך קטגוריית ברירת מחדל.
- **תנאים לסיום המשימה (Definition of Done)**:
  1. אפשרות ליצור, לערוך ולמחוק קטגוריות אב ותתי-קטגוריות.
  2. מחיקת קטגוריה עם תנועות קיימות מחייבת בחירת קטגוריה חלופית להעברת התנועות.
  3. מיזוג שני בתי עסק מעדכן את כל התנועות הקשורות לרשומה המאוחדת.
  4. כל התצוגה מבוססת אייקונים וצבע בלבד ללא אימוג'ים.

---

#### 📌 TASK-06: ניהול אמצעי תשלום, חשבונות בנק וארנקים
- **עדיפות**: גבוהה
- **מורכבות**: בינונית
- **ספרינט**: Sprint 2
- **שכבות**: `features/accounts/` (Data, Domain, Presentation)
- **ישויות קשורות**: `Account`, `AccountType`
- **מסכים רלוונטיים**: מסך 7 (רשימה ופירוט חשבונות ואמצעי תשלום)
- **סעיף ב-SDD**: 4.10
- **תיאור המשימה**:
  - מודול ניהול חשבונות: בנק (עו"ש), כרטיס אשראי, ארנק דיגיטלי, מזומן.
  - שדות: שם, סוג, מטבע בסיס, יתרה ראשונית/נוכחית, אייקון וצבע זיהוי.
  - עבור כרטיסי אשראי: הגדרת יום חיוב חודשי וקישור לחשבון הבנק הממנו מתבצע החיוב.
  - אפשרות לארכוב/הסתרת חשבון ללא מחיקת היסטוריית התנועות.
  - מסך פירוט חשבון: יתרה נוכחית, גרף מגמת יתרה לאורך זמן, ורשימת תנועות מסוננת לפי החשבון.
- **תנאים לסיום המשימה (Definition of Done)**:
  1. יצירה ועריכה של חשבונות מכל ארבעת הסוגים.
  2. יתרת החשבון מתעדכנת בזמן אמת עם כל תנועה חדשה, עריכה או מחיקה.
  3. ארכוב חשבון מסיר אותו ממסך ההזנה המהיר אך משמר את כל ההיסטוריה בדוחות.
  4. בדיקות יחידה לחישוב יתרות חשבון לפי תנועות היסטוריות.

---

#### 📌 TASK-07: מודול הזנת עסקה מהירה (Optimistic UI)
- **עדיפות**: קריטית
- **מורכבות**: בינונית-גבוהה
- **ספרינט**: Sprint 2-3
- **שכבות**: `features/transactions/` (Data, Domain, Presentation)
- **ישויות קשורות**: `Transaction`, `Account`, `Category`, `Merchant`
- **מסכים רלוונטיים**: מסך 2 (הזנה מהירה - Modal/BottomSheet), מסך 4 (פרטי תנועה ועריכה)
- **סעיף ב-SDD**: 4.1, 6.1
- **תיאור המשימה**:
  - יצירת דיאלוג/BottomSheet מהיר להזנת עסקה הנגיש בכל רגע מכל מסך דרך כפתור ה-FAB או קיצור דרך.
  - שדות חובה: סכום, סוג (הוצאה/הכנסה), חשבון מקור, תאריך (ברירת מחדל: היום).
  - שדות אופציונליים: קטגוריה, בית עסק, תגיות, הערה.
  - השלמה אוטומטית חכמה של בית עסק וסיווג אוטומטי של הקטגוריה בהתבסס על היסטוריית שימוש.
  - ביצוע בגישת **Optimistic UI**: עדכון ה-State המקומי וה-UI מיידית ללא השהייה או רענון מסך מלא.
- **תנאים לסיום המשימה (Definition of Done)**:
  1. הזנת עסקה מלאה אפשרית בפחות מ-10 שניות (מקלדת נפתחת ישירות לסכום).
  2. הקלדת שם בית עסק קיים מציעה השלמה אוטומטית וממלאת אוטומטית את הקטגוריה המקושרת.
  3. שמירה סוגרת את המסך מיידית והעסקה מופיעה בראש רשימת התנועות.
  4. ולידציות תקינות: מניעת סכום אפס או שלילי, חובת בחירת חשבון ותאריך.

---

#### 📌 TASK-08: מודול פיצול עסקה למספר קטגוריות (Transaction Splits)
- **עדיפות**: גבוהה
- **מורכבות**: בינונית-גבוהה
- **ספרינט**: Sprint 3
- **שכבות**: `features/transactions/` (Splits Domain & UI)
- **ישויות קשורות**: `Transaction`, `TransactionSplit`, `Category`
- **מסכים רלוונטיים**: מסך 2, מסך 4
- **סעיף ב-SDD**: 4.5
- **תיאור המשימה**:
  - פיתוח מנגנון פיצול תנועה בודדת (למשל קבלה מסופרמרקט) למספר שורות עם קטגוריות שונות, סכומים נפרדים והערות אישיות.
  - ולידציה בזמן אמת: הצגת "סכום שנותר לסיווג" וחסימת שמירה עד שסכום הפיצולים מסתכם בדיוק לסכום העסקה הכולל.
  - תצוגה ברשימת התנועות כרשומה מאוחדת עם אינדיקציה חזותית ברורה (אייקון פיצול) ואפשרות להרחבה (Expand/Collapse).
  - התאמת שאילתות התקציב, הדוחות והפילוחים כך שיתייחסו לכל שורת פיצול כרשומה קטגוריאלית עצמאית.
- **תנאים לסיום המשימה (Definition of Done)**:
  1. לא ניתן לשמור פיצול שסכומו אינו תואם בדיוק את סכום העסקה.
  2. דוחות תקציב מחשבים את החלק היחסי של כל קטגוריה בפיצול.
  3. עריכה או מחיקה של עסקת אב מעדכנת/מוחקת את שורות הפיצול בהתאמה ב-DB (Cascade).

---

#### 📌 TASK-09: מודול העברות פנימיות בין חשבונות והמרות שער
- **עדיפות**: גבוהה
- **מורכבות**: בינונית
- **ספרינט**: Sprint 3
- **שכבות**: `features/transactions/` (Transfer Domain & UI)
- **ישויות קשורות**: `Transaction`, `TransferLink`, `Account`, `ExchangeRate`
- **מסכים רלוונטיים**: מסך 6 (העברה בין חשבונות)
- **סעיף ב-SDD**: 4.4, 4.26
- **תיאור המשימה**:
  - רישום תנועת העברה בין שני חשבונות (למשל מעו"ש לחיסכון או מחיסכון לתיק השקעות).
  - יצירת צמד תנועות מקושרות בטבלת `TransferLinksTable` (חיוב במקור, זיכוי ביעד).
  - החרגה גורפת של תנועות העברה פנימית מדוחות הוצאות, הכנסות, תקציב ודשבורד תובנות.
  - תמיכה בהעברה בין חשבונות במטבעות שונים עם הזנת שער המרה או חישוב סכום יעד.
  - סנכרון דו-כיווני: עריכת סכום, תאריך או מחיקת צד אחד מעדכנת/מוחקת אוטומטית את הצד השני.
- **תנאים לסיום המשימה (Definition of Done)**:
  1. העברה יוצרת תנועת משיכה ותנועת הפקדה בשני החשבונות בהתאמה.
  2. סך ההוצאות וההכנסות החודשי בדשבורד אינו משתנה כתוצאה מהעברה פנימית.
  3. מחיקת העברה מחזירה את היתרות בשני החשבונות למצבן הקודם.

---

#### 📌 TASK-10: ניהול עסקאות בתשלומים ופירוק לחיובים עתידיים
- **עדיפות**: גבוהה
- **מורכבות**: גבוהה
- **ספרינט**: Sprint 3
- **שכבות**: `features/transactions/` (Installments Engine & UI)
- **ישויות קשורות**: `Transaction`, `InstallmentPlan`, `InstallmentItem`, `Account`
- **מסכים רלוונטיים**: מסך 2, מסך 4
- **סעיף ב-SDD**: 4.2
- **תיאור המשימה**:
  - מודול עסקאות תשלומים: סכום כולל, מספר תשלומים, תאריך תשלום ראשון, סכום תשלום ראשון נפרד (אם קיים הבדל עמלה).
  - יצירה אוטומטית של רשומות `InstallmentItem` לכל חודש עתידי בהתאם ליום החיוב של הכרטיס.
  - לוגיקה עסקית: תשלומים עתידיים משתקפים בתחזית התזרים ולוח האשראי אך נספרים בהוצאות בפועל רק בחודש החיוב שלהם.
  - אפשרות עריכה או ביטול מוקדם של יתרת התשלומים (עקב ביטול עסקה או זיכוי).
- **תנאים לסיום המשימה (Definition of Done)**:
  1. יצירת עסקת 12 תשלומים מייצרת בדיוק 12 פריטים עם תאריכי חיוב עוקבים.
  2. בדוח חודשי שוטף מופיע רק התשלום השייך לאותו החודש.
  3. בלוח חיובי אשראי ותזרים מזומנים נראים כלל החיובים העתידיים.

---

#### 📌 TASK-11: מודול הוראות קבע, מנויים מחזוריים והתרעות שינוי
- **עדיפות**: גבוהה
- **מורכבות**: בינונית-גבוהה
- **ספרינט**: Sprint 3-4
- **שכבות**: `features/transactions/` (Recurring Engine & UI)
- **ישויות קשורות**: `RecurringRule`, `Transaction`, `Account`, `Category`
- **מסכים רלוונטיים**: מסך 5 (רשימה וניהול הוראות קבע)
- **סעיף ב-SDD**: 4.3
- **תיאור המשימה**:
  - ניהול הוראות קבע ומנויים (שכירות, אינטרנט, סטרימינג, ביטוחים): סכום, תדירות (חודשי/דו-חודשי/רבעוני/שנתי), יום בחודש, חשבון חיוב, תאריך התחלה וסיום אופציונלי.
  - מנוע יצירת תנועות אוטומטי במועד החיוב או הצגת תנועה לאישור ידני (בהתאם להגדרת המשתמש).
  - מנגנון השהייה זמנית (Pause/Resume) של מנוי ללא מחיקת היסטוריה.
  - מנגנון התרעה חכמה: זיהוי שינוי סכום חריג בחיוב מחזורי מאותו בית עסק.
  - מסך ניהול המציג ריכוז סך ההתחייבויות הקבועות החודשיות של המשתמש.
- **תנאים לסיום המשימה (Definition of Done)**:
  1. בהגיע מועד הריצה, התנועה נוצרת או מוצגת ברשימת האישורים.
  2. השהיית הוראת קבע מונעת יצירת תנועות עתידיות ומסומנת חזותית במסך.
  3. סך ההתחייבויות החודשי מסתכם בצורה מדויקת כולל נרמול תשלומים שנתיים לחודש.

---

#### 📌 TASK-12: מסך רשימת תנועות, חיפוש חופשי, סינון מתקדם ועריכה מרובה
- **עדיפות**: גבוהה
- **מורכבות**: גבוהה
- **ספרינט**: Sprint 4
- **שכבות**: `features/transactions/` (Presentation & Queries)
- **ישויות קשורות**: `Transaction`, `SavedFilter`
- **מסכים רלוונטיים**: מסך 3 (רשימת תנועות וסינון), מסך 4 (עריכת תנועה)
- **סעיף ב-SDD**: 4.9, 6.1
- **תיאור המשימה**:
  - פיתוח מסך רשימת תנועות מרכזי עם חלוקה לפי חודשים/ימים וביצועים מותאמים ל-10,000+ רשומות (Virtual Scrolling / Pagination).
  - מנוע חיפוש טקסט חופשי מהיר (Full Text Search) על פני סכום, בית עסק, הערה וקטגוריה.
  - סרגל סינון מתקדם: טווח תאריכים, חשבונות, קטגוריות, תגיות, טווח סכומים וסוג עסקה (רגילה/תשלומים/הוראת קבע/מפוצלת).
  - שמירת תבניות סינון שכיחות כ"תצוגות מועדפות" (Saved Filters).
  - מצב בחירה מרובה (Multi-Select): שינוי קטגוריה קבוצתי, הוספת תגית מרובה, מחיקה קבוצתית.
  - עריכת שדה בודד ישירות מהרשימה (Inline Edit) בסביבת דסקטופ/טאבלט.
- **תנאים לסיום המשימה (Definition of Done)**:
  1. גלילה חלקה של 10,000 רשומות ללא קיטועים (60fps).
  2. תוצאות חיפוש מופיעות תוך פחות מ-100ms מרגע ההקלדה.
  3. ביצוע פעולה מרובה (למשל שינוי קטגוריה ל-50 תנועות) מתבצע בטרנזקציית DB אחת ומעדכן את המסך מיד.

---

### Phase 3: מנוע ייבוא חכם, זיהוי כפילויות וסיווג אוטומטי

---

#### 📌 TASK-13: מנוע ייבוא קובצי Excel/CSV מבנקים וחברות אשראי
- **עדיפות**: קריטית
- **מורכבות**: גבוהה
- **ספרינט**: Sprint 5
- **שכבות**: `features/import_export/` (Parsers & Mappings)
- **ישויות קשורות**: `ImportBatch`, `ImportMapping`, `Transaction`
- **מסכים רלוונטיים**: מסך 8 (העלאה, מיפוי, תצוגה מקדימה ואישור ייבוא)
- **סעיף ב-SDD**: 4.6, נספח 7.2
- **תיאור המשימה**:
  - פיתוח Parser ייעודי לקובצי Excel (`.xlsx`) ו-CSV עבור המוסדות הפיננסיים בישראל:
    1. **ישראכרט** (פירוט חיובים בארץ ובחו"ל).
    2. **בנק לאומי** (דוח תנועות עו"ש).
    3. **בנק פאג"י / הבנק הבינלאומי** (דוח תנועות עו"ש).
    4. **OneZero** (דוח תנועות וכרטיסים).
  - מסך מיפוי עמודות חכם (תאריך, סכום חובה/זכות, בית עסק, מספר אסמכתא) עם שמירת התבנית לשימוש חוזר.
  - מסך תצוגה מקדימה (Preview) המציג את הנתונים המעובדים, שגיאות בפורמט ושורות חסרות לתיקון ידני לפני אישור.
  - ניהול אצוות ייבוא (`ImportBatch`) המאפשרת **Rollback מלא** (ביטול ייבוא ומחיקת כל התנועות שנוצרו באצווה בלחיצה אחת).
- **תנאים לסיום המשימה (Definition of Done)**:
  1. קובץ דוח לדוגמה מכל אחד מ-4 המקורות מיובא בהצלחה ללא שגיאות.
  2. תבנית מיפוי עמודות נשמרת אוטומטית לפי שם הקובץ/מקור.
  3. ביצוע Rollback מוחק בדיוק את תנועות האצווה ומחזיר את יתרות החשבון לקדמותן.

---

#### 📌 TASK-14: אלגוריתם זיהוי כפילויות ואיחוד עסקאות בייבוא (Deduplication)
- **עדיפות**: גבוהה
- **מורכבות**: גבוהה
- **ספרינט**: Sprint 5
- **שכבות**: `features/import_export/` (Deduplication Engine)
- **ישויות קשורות**: `Transaction`, `ImportBatch`, `MatchCandidate`
- **מסכים רלוונטיים**: מסך 8 (תצוגת איחוד כפילויות)
- **סעיף ב-SDD**: 4.7
- **תיאור המשימה**:
  - פיתוח אלגוריתם לזיהוי כפילויות בין עסקאות שיובאו מהקובץ לעסקאות קיימות במערכת (שהוקלדו ידנית או יובאו בעבר).
  - קריטריוני התאמה:
    - **התאמה ודאית (Exact/High Confidence)**: חשבון זהה + סכום זהה + תאריך זהה (או סטייה של עד יום 1) + דמיון גבוה בבית עסק -> איחוד אוטומטי.
    - **התאמה סבירה (Medium Confidence / Fuzzy Match)**: סכום זהה + חלון תאריכים קרוב + דמיון חלקי בשם (למשל "שופרסל דיל" מול "שופרסל בעמ") -> הצגה למשתמש לבחירה/אישור.
    - **התאמה נמוכה (Low Confidence)**: ייבוא כרשומה חדשה נפרדת.
  - לוגיקת שימור נתונים באיחוד: שמירת הקטגוריה, התגית וההערה שהוזנו ידנית, ועדכון שדות אסמכתא ומקור מהדוח הבנקאי.
  - ניהול יומן איחודים (Merge Log) עם אפשרות ביטול איחוד שגוי.
- **תנאים לסיום המשימה (Definition of Done)**:
  1. עסקאות שהוזנו ידנית מזוהות ככפולות בעת ייבוא דוח כרטיס אשראי ומאוחדות ללא שכפול סכומים.
  2. מסך התצוגה המקדימה מסמן בצורה מובחנת עסקאות חדשות, מאוחדות ועסקאות הדורשות אישור.
  3. בדיקות יחידה מקיפות לאלגוריתם ה-Fuzzy Match עם מקרי קצה שונים.

---

#### 📌 TASK-15: מנוע הצעות סיווג אוטומטי מבוסס למידה מקומית
- **עדיפות**: גבוהה
- **מורכבות**: בינונית-גבוהה
- **ספרינט**: Sprint 6
- **שכבות**: `features/auto_categorization/` (Rules & Learning Engine)
- **ישויות קשורות**: `Merchant`, `CategoryRule`, `Transaction`, `Category`
- **מסכים רלוונטיים**: מסך 9 (עסקאות לסיווג - הצעות אוטומטיות)
- **סעיף ב-SDD**: 4.8
- **תיאור המשימה**:
  - פיתוח מנוע סיווג מקומי הלומד מדפוסי סיווג עבר של המשתמש:
    - כלל סטטיסטי: אם בית עסק X סווג בעבר בלפחות 90% מהמקרים לקטגוריה Y -> הצע אוטומטית את קטגוריה Y.
  - תמיכה בהגדרת כללי סיווג ידניים קבועים (לדוגמה: "כל תנועה המכילה 'פז' תסווג לרכב ודלק").
  - מסך ייעודי "עסקאות לסיווג" (מסך 9) המרכז תנועות חדשות ללא קטגוריה עם הצעות המנוע, המאפשר אישור בלחיצה אחת (`One-Tap Batch Approve`) או שינוי מהיר.
  - שיפור דיוק המנוע מקומית עם כל אישור/דחייה, ללא שום שליחת נתונים לשרת חיצוני.
- **תנאים לסיום המשימה (Definition of Done)**:
  1. ייבוא 100 עסקאות מחיל אוטומטית קטגוריות על בתי עסק מוכרים.
  2. מסך עסקאות לסיווג מאפשר לאשר את כל ההצעות בבת אחת או לתקן שורה בודדת.
  3. כלל ידני חדש מוחל רטרואקטיבית על תנועות קיימות ללא סיווג לפי בחירת המשתמש.

---

### Phase 4: תקציב, ניצול תקציב, חיובי אשראי ותזרים מזומנים

---

#### 📌 TASK-16: מודול תכנון תקציב חודשי לפי קטגוריות
- **עדיפות**: גבוהה
- **מורכבות**: בינונית
- **ספרינט**: Sprint 6
- **שכבות**: `features/budgets/` (Budget Planning Domain & UI)
- **ישויות קשורות**: `Budget`, `Category`, `BudgetPeriod`
- **מסכים רלוונטיים**: מסך 12 (תכנון תקציב חודשי)
- **סעיף ב-SDD**: 4.13
- **תיאור המשימה**:
  - מסך תכנון תקציב חודשי המאפשר הגדרת תקרת הוצאה לכל קטגוריית אב או תת-קטגוריה.
  - תמיכה בחישוב הצעת תקציב חכמה: הצגת ממוצע ההוצאות בפועל ב-3 החודשים האחרונים באותה קטגוריה.
  - תמיכה בתקציב קבוע (ידני) מול תקציב גמיש.
  - כפתור שכפול מהיר של תבנית התקציב מחודש קודם בלחיצה אחת.
  - סיכום תקציב כולל מול הכנסות צפויות לצורך הצגת מאזן תקציבי מתוכנן.
- **תנאים לסיום המשימה (Definition of Done)**:
  1. יצירת תקציב מלא לחודש נתון ושמירתו ב-DB.
  2. שכפול תקציב מחודש קודם מעתיק את כל הקטגוריות והסכומים באופן מדויק.
  3. הצעת התקציב מחשבת נכון את הממוצע ההיסטורי של 3 החודשים האחרונים.

---

#### 📌 TASK-17: מנגנון גלגול יתרות תקציב מחודש לחודש (Rollover Budget)
- **עדיפות**: בינונית-גבוהה
- **מורכבות**: בינונית
- **ספרינט**: Sprint 6-7
- **שכבות**: `features/budgets/` (Rollover Logic)
- **ישויות קשורות**: `Budget`, `BudgetPeriod`, `Category`
- **מסכים רלוונטיים**: מסך 12, מסך 13
- **סעיף ב-SDD**: 4.15
- **תיאור המשימה**:
  - פיתוח מנגנון גלגול יתרות תקציב (Rollover) ברמת קטגוריה:
    - עודף תקציבי מחודש קודם מתווסף לתקרת התקציב בחודש הנוכחי.
    - גירעון (חריגה) מחודש קודם מופחת מתקרת התקציב בחודש הנוכחי.
  - תצוגה שקופה ומפורטת במסך התקציב: הצגת "תקציב בסיס", "יתרה מגולגלת" ו"תקציב אפקטיבי זמין".
  - אפשרות להגדרת תקרת מקסימום לגלגול מצטבר למניעת ניפוח בלתי מוגבל.
- **תנאים לסיום המשימה (Definition of Done)**:
  1. מעבר חודש מעדכן את התקציב האפקטיבי של קטגוריות עם גלגול פעיל.
  2. קטגוריות ללא גלגול מתאפסות בכל חודש לתקציב הבסיס המקורי.
  3. בדיקות יחידה המכסות תרחישי עודף, גירעון והגבלת תקרה מקסימלית.

---

#### 📌 TASK-18: מעקב ניצול תקציב, קצב שריפה והתרעות חריגה
- **עדיפות**: גבוהה
- **מורכבות**: בינונית-גבוהה
- **ספרינט**: Sprint 7
- **שכבות**: `features/budgets/` (Tracking & Notifications)
- **ישויות קשורות**: `Budget`, `Transaction`, `Notification`
- **מסכים רלוונטיים**: מסך 13 (מעקב ניצול תקציב)
- **סעיף ב-SDD**: 4.14
- **תיאור המשימה**:
  - פיתוח מסך מעקב ניצול תקציב חודשי בזמן אמת.
  - פסי התקדמות חזותיים מקודדי צבע:
    - **ירוק**: ניצול של עד 70%.
    - **כתום**: ניצול של 70% עד 100%.
    - **אדום**: חריגה מעל 100%.
  - מדד "קצב שריפה יחסי" (Pace / Burn Rate): האם קצב ההוצאה עד כה בחודש תואם את היום בחודש או צפוי להוביל לחריגה.
  - מנגנון התרעות מקומיות (Local Notifications) בהגעה ל-80% מניצול התקציב ובחריגה מלאה.
  - מסך סיכום חודשי המרכז את כלל הקטגוריות שחרגו.
- **תנאים לסיום המשימה (Definition of Done)**:
  1. עדכון עסקה מעדכן מיידית את פסי הניצול וקצב השריפה במסך.
  2. התרעה מקומית מופעלת בדיוק בחציית סף הניצול שהוגדר.
  3. הצגת נתונים נכונה כולל שורות פיצול עסקאות (Splits).

---

#### 📌 TASK-19: לוח חיזוי וריכוז מועדי חיובי כרטיסי אשראי
- **עדיפות**: גבוהה
- **מורכבות**: בינונית
- **ספרינט**: Sprint 7
- **שכבות**: `features/cash_flow/` (Credit Card Forecast)
- **ישויות קשורות**: `Account`, `Transaction`, `InstallmentItem`, `RecurringRule`
- **מסכים רלוונטיים**: מסך 10 (לוח חיובי אשראי)
- **סעיף ב-SDD**: 4.11
- **תיאור המשימה**:
  - מסך המרכז את כל כרטיסי האשראי של המשתמש ומחשב את סכום החיוב הצפוי בכל כרטיס למועד הסליקה הקרוב.
  - פירוט מלא של רכיבי החיוב: עסקאות שוטפות במחזור, תשלומים עתידיים, והוראות קבע בכרטיס.
  - השוואת סכום החיוב הצפוי מול החיוב בפועל בחודש הקודם והדגשת חריגות.
  - מנגנון התרעה מוקדמת מוגדרת אישית (X ימים לפני מועד החיוב בחשבון הבנק).
- **תנאים לסיום המשימה (Definition of Done)**:
  1. חישוב מדויק של סך החיוב הצפוי בהתאם לתאריך תחילת וסיום מחזור הסליקה של כל כרטיס.
  2. פירוט כל התנועות המרכיבות את החיוב בלחיצה על כרטיס.
  3. התרעה נשלחת במועד המבוקש למניעת הפתעות בעו"ש.

---

#### 📌 TASK-20: מנוע תחזית תזרים מזומנים עתידי (30/60/90 יום) ותרחישי "מה אם"
- **עדיפות**: גבוהה
- **מורכבות**: גבוהה
- **ספרינט**: Sprint 7-8
- **שכבות**: `features/cash_flow/` (Cash Flow Engine & Charts)
- **ישויות קשורות**: `Transaction`, `RecurringRule`, `InstallmentItem`, `CashFlowSnapshot`
- **מסכים רלוונטיים**: מסך 11 (תחזית תזרים מזומנים)
- **סעיף ב-SDD**: 4.12
- **תיאור המשימה**:
  - מנוע חישוב יתרת עו"ש צפויה קדימה בזמן ל-30, 60 ו-90 יום.
  - מקורות מידע לתחזית: הכנסות קבועות (משכורות), הוראות קבע, תשלומים עתידיים וממוצע הוצאות משתנות היסטוריות.
  - גרף קו אינטראקטיבי המציג את מגמת היתרה הצפויה לאורך ציר הזמן עם הדגשת "נקודות סיכון" (Cash Flow Risk / צפי ליתרה שלילית).
  - מודול סימולציית תרחישי "מה אם" (What-If Scenarios): הוספת הכנסה או הוצאה עתידית חד-פעמית משוערת וצפייה בהשפעתה על התזרים בזמן אמת.
- **תנאים לסיום המשימה (Definition of Done)**:
  1. גרף התזרים מציג במדויק את יתרת הפתיחה, ימי החיוב והיתרה הסופית.
  2. התרעה חזותית בולטת במידה והיתרה צפויה לרדת מתחת לאפס או למסגרת.
  3. תרחיש "מה אם" ניתן להפעלה וכיבוי בלחיצה אחת ומעדכן את הגרף מיידית.

---

### Phase 5: השקעות, נכסים פנסיוניים, התחייבויות וסנכרון שערים

---

#### 📌 TASK-21: שירות סנכרון היברידי וחיבור ל-Finnhub API (מניות ומט"ח)
- **עדיפות**: גבוהה
- **מורכבות**: בינונית-גבוהה
- **ספרינט**: Sprint 8
- **שכבות**: `core/services/sync/`, `data/datasources/remote/`
- **ישויות קשורות**: `PriceQuote`, `ExchangeRate`, `SyncLog`
- **סעיף ב-SDD**: 2.3, 4.19, נספח 7.1
- **תיאור המשימה**:
  - פיתוח שירות סנכרון רקע היברידי (`HybridSyncService`) המזהה חיבור רשת פעיל.
  - אינטגרציה עם **Finnhub Stock API** (שימוש ב-API Key: `da45eehr01qo2j872nb0da45eehr01qo2j872nbg`) למשיכת ציטוטי מניות ושערי חליפין (USD/ILS, EUR/ILS וכו').
  - שמירת השערים האחרונים הידועים במסד הנתונים המקומי (`PriceQuotesTable`, `ExchangeRatesTable`) לשימוש מלא במצב Offline.
  - חותמת זמן לכל שער והצגת אינדיקציה חזותית ברורה ("מעודכן לתאריך X") בכל מקום בו מוצג שווי מבוסס שער ישן.
  - מנגנון Retry & Exponential Backoff בעת כשל תקשורת מבלי לחסום את ממשק המשתמש.
- **תנאים לסיום המשימה (Definition of Done)**:
  1. משיכת שערים תקינה מ-Finnhub API ושמירתם ב-DB המקומי.
  2. ניתוק הרשת (מצב טיסה) משאיר את האפליקציה פונקציונלית לחלוטין ומציג שערים אחרונים עם תאריך עדכון.
  3. רענון ידני (Pull-to-refresh) מעדכן את השערים ומציג הודעת הצלחה/שגיאה עדינה.

---

#### 📌 TASK-22: מודול ניהול תיק מניות, קרנות סל ומדדי ייחוס
- **עדיפות**: גבוהה
- **מורכבות**: גבוהה
- **ספרינט**: Sprint 9
- **שכבות**: `features/investments/` (Stock Portfolio Domain & UI)
- **ישויות קשורות**: `Security`, `Holding`, `InvestmentTransaction`, `PriceQuote`
- **מסכים רלוונטיים**: מסך 14 (רשימת אחזקות - תיק השקעות), מסך 15 (פרטי אחזקה ועסקת השקעה)
- **סעיף ב-SDD**: 4.16, נספח 7.5
- **תיאור המשימה**:
  - מודול לניהול עסקאות קנייה ומכירה של ניירות ערך (מניות, קרנות סל, אג"ח): טיקר, כמות, מחיר, עמלה, תאריך ומטבע מסחר.
  - חישוב אוטומטי של עלות בסיס ממוצעת (Average Cost Basis) לכל פוזיציה.
  - חישוב והצגת רווח/הפסד לא ממומש (Unrealized P&L) ורווח/הפסד ממומש (Realized P&L).
  - השוואת תשואת התיק מול מדדי ייחוס סגורים: **S&P 500**, **ת"א 125**, **נאסד"ק 100**.
  - מסך פירוט אחזקה: גרף שווי, רשימת עסקאות קנייה/מכירה, ותשואה נומינלית מול ריאלית.
- **תנאים לסיום המשימה (Definition of Done)**:
  1. הזנת רכישות מרובות באותו טיקר מחשבת במדויק את עלות הבסיס הממוצעת והרווח הלא ממומש.
  2. מכירת חלק מהפוזיציה מחשבת נכון את הרווח הממומש ומעדכנת את יתרת האחזקה.
  3. גרף השוואתי מציג את תשואת התיק מול המדד הנבחר.

---

#### 📌 TASK-23: מודול מעקב נכסים פנסיוניים, קרנות השתלמות וקופות גמל
- **עדיפות**: בינונית-גבוהה
- **מורכבות**: בינונית
- **ספרינט**: Sprint 9
- **שכבות**: `features/pension_assets/` (Pension Domain & UI)
- **ישויות קשורות**: `PensionAsset`, `PensionValueSnapshot`
- **מסכים רלוונטיים**: מסך 16 (נכסים פנסיוניים)
- **סעיף ב-SDD**: 4.17
- **תיאור המשימה**:
  - מודול למעקב אחר נכסים פיננסיים פנסיוניים ארוכי טווח: קרן פנסיה, קרן השתלמות, קופת גמל, ביטוח מנהלים.
  - שדות: סוג מוצר, חברה מנהלת, מסלול השקעה, יתרה עדכנית, תאריך עדכון אחרון.
  - מנגנון עדכון יתרה תקופתי (רבעוני/שנתי) עם שמירת היסטוריית תמונות מצב (Snapshots) לצורך גרף מגמת צבירה לאורך זמן.
  - חישוב הפקדות חודשיות צפויות (עובד ומעביד) לצורך הערכת קצב גידול.
  - שיוך הנכסים בנפרד מנכסים נזילים לצורך דוחות שווי נקי.
- **תנאים לסיום המשימה (Definition of Done)**:
  1. יצירה ועדכון יתרות לכל מוצר פנסיוני.
  2. גרף היסטוריית צבירה מציג את השינוי בין הדוחות התקופתיים שהוזנו.
  3. הפרדה ברורה בדוחות בין נכסים פנסיוניים נעולים לבין נכסים נזילים.

---

#### 📌 TASK-24: מודול נכסים לא סחירים, הלוואות, משכנתאות ולוחות סילוקין
- **עדיפות**: גבוהה
- **מורכבות**: גבוהה
- **ספרינט**: Sprint 10
- **שכבות**: `features/non_market_assets/` (Assets & Liabilities Engine)
- **ישויות קשורות**: `Asset`, `Liability`, `LoanSchedule`
- **מסכים רלוונטיים**: מסך 17 (נכסים לא סחירים והתחייבויות)
- **סעיף ב-SDD**: 4.18
- **תיאור המשימה**:
  - ניהול נכסים לא סחירים (נדל"ן, רכב, פריטי ערך): שווי מוערך, תאריך הערכה אחרון.
  - ניהול התחייבויות (הלוואות, משכנתאות): יתרת קרן, שיעור ריבית, תשלום חודשי, תקופה שנותרה.
  - מנוע חישוב לוח סילוקין אוטומטי (שפיצר / קרן שווה) ועדכון יתרת הקרן עם כל תשלום חודשי שבוצע.
  - תמיכה במשכנתה מרובת מסלולים (למשל: פריים + קבועה צמודה + משתנה לא צמודה) תחת נכס יחיד.
  - חישוב הון עצמי נקי בנכס (Home Equity = שווי הנכס פחות יתרת המשכנתה).
- **תנאים לסיום המשימה (Definition of Done)**:
  1. לוח סילוקין שפיצר מחושב במדויק (קרן מול ריבית חודשית).
  2. משכנתה עם 3 מסלולים מציגה את סך ההחזר החודשי המאוחד ואת יתרת החוב הכוללת.
  3. שווי ההון העצמי מתעדכן אוטומטית בהתאם להפחתת הקרן ועדכון שווי הנכס.

---

#### 📌 TASK-25: מודול ניהול רב-מטבעי, שערים היסטוריים והמרות
- **עדיפות**: בינונית-גבוהה
- **מורכבות**: בינונית-גבוהה
- **ספרינט**: Sprint 10
- **שכבות**: `features/investments/` (Multi-Currency Logic)
- **ישויות קשורות**: `ExchangeRate`, `Transaction`, `InvestmentTransaction`, `Currency`
- **מסכים רלוונטיים**: מסך 15, דוחות כלליים
- **סעיף ב-SDD**: 4.26
- **תיאור המשימה**:
  - תמיכה בחשבונות, תנועות והשקעות במטבעות שונים (ILS, USD, EUR, GBP וכו').
  - שמירת שער ההמרה ההיסטורי המדויק ביום ביצוע כל עסקה.
  - נרמול כלל הדוחות, התקציבים ודשבורד השווי הנקי למטבע בסיס ראשי אחד (שקל כברירת מחדל).
  - במסך פירוט אחזקה בניירות ערך זרים: הצגת רווח/הפסד במטבע המקור (דולר) לצד רווח/הפסד במטבע הבסיס (שקל) כדי להבחין בין רווח ממחיר המניה לרווח משינוי שער חליפין.
  - אפשרות להזנת שער חליפין ידני למטבעות שאינם נתמכים אוטומטית.
- **תנאים לסיום המשימה (Definition of Done)**:
  1. עסקה במטבע זר שומרת את סכום המקור ושער ההמרה לשקלים באותו היום.
  2. דוח השווי הנקי ממיר את כל הנכסים למטבע הבסיס בצורה עקבית ומדויקת.
  3. הפרדת הרווח הדולרי מהרווח השקלי במסך פירוט מניה.

---

### Phase 6: דשבורדים, שווי נקי, תובנות וניתוחים פיננסיים

---

#### 📌 TASK-26: מנוע חישוב ודשבורד שווי נקי כולל (Net Worth Dashboard)
- **עדיפות**: קריטית
- **מורכבות**: גבוהה
- **ספרינט**: Sprint 11
- **שכבות**: `features/net_worth/` (Domain & Presentation)
- **ישויות קשורות**: `NetWorthSnapshot`, `Account`, `Holding`, `PensionAsset`, `Asset`, `Liability`
- **מסכים רלוונטיים**: מסך 18 (שווי נקי כולל)
- **סעיף ב-SDD**: 4.20
- **תיאור המשימה**:
  - מנוע איחוד וחישוב שווי נקי כולל:
    $$\text{Net Worth} = (\text{עו"ש וארנקים} + \text{תיק השקעות} + \text{פנסיה וגמל} + \text{נדל"ן ונכסים}) - (\text{אשראי} + \text{הלוואות} + \text{משכנתאות})$$
  - פילוח ויזואלי אינטראקטיבי (גרף עוגה / מדרגות) לפי קטגוריות נכסים והתחייבויות.
  - יצירת תמונות מצב תקופתיות (`NetWorthSnapshots`) בסוף כל חודש או בשינוי מהותי.
  - גרף מגמת שווי נקי לאורך זמן (חודשי/שנתי).
  - מתג סינון: הצגת "שווי נקי נזיל" (החרגת נדל"ן ופנסיה) לתמונת מצב שמרנית.
- **תנאים לסיום המשימה (Definition of Done)**:
  1. חישוב מדויק של כל רכיבי המאזן על בסיס נתוני האמת מכל המודולים.
  2. גרף המגמה מציג בצורה ברורה את השינוי בשווי הנקי מחודש לחודש.
  3. מעבר בין שווי כולל לשווי נזיל מעדכן את כל הגרפים והמספרים במסך.

---

#### 📌 TASK-27: פילוח הוצאות רב-ממדי: צרכים מול מותרות, קבועות מול משתנות
- **עדיפות**: גבוהה
- **מורכבות**: בינונית-גבוהה
- **ספרינט**: Sprint 11
- **שכבות**: `features/insights_analytics/` (Spending Classification)
- **ישויות קשורות**: `Category`, `Transaction`, `SpendingClassification`
- **מסכים רלוונטיים**: מסך 19 (ניתוח מגמות והשוואות)
- **סעיף ב-SDD**: 4.22
- **תיאור המשימה**:
  - מודול ניתוח הוצאות בצירים מקבילים:
    1. צרכים (Needs) מול מותרות (Wants) - מודל 50/30/20.
    2. הוצאות קבועות (Fixed) מול הוצאות משתנות (Variable).
  - תיוג ברירת מחדל ברמת קטגוריה ואפשרות דריסה ברמת התנועה הבודדת.
  - גרפי התפלגות חודשית והצגת מגמת שינוי ביחס צרכים/מותרות לאורך זמן.
  - התרעה רכה כאשר שיעור המותרות עולה מעל הממוצע האישי ההיסטורי של המשתמש.
- **תנאים לסיום המשימה (Definition of Done)**:
  1. פילוח כלל הוצאות החודש לפי שני הצירים.
  2. שינוי תיוג בתנועה בודדת משפיע ישירות על תוצאות הדוח.
  3. גרפיקה נקייה ואחידה ללא שימוש באימוג'ים.

---

#### 📌 TASK-28: מנוע ניתוח מגמות השוואתי בין תקופות
- **עדיפות**: גבוהה
- **מורכבות**: בינונית-גבוהה
- **ספרינט**: Sprint 12
- **שכבות**: `features/insights_analytics/` (Comparative Analytics)
- **ישויות קשורות**: `Transaction`, `Category`, `PeriodicSummary`
- **מסכים רלוונטיים**: מסך 19 (ניתוח מגמות והשוואות)
- **סעיף ב-SDD**: 4.23
- **תיאור המשימה**:
  - מודול השוואה פיננסית מתקדם בין תקופות מקבילות:
    - חודש מול חודש קודם / חודש מקביל אשתקד.
    - רבעון מול רבעון.
    - שנה מול שנה.
  - בחירת מדד להשוואה: סך הוצאות, סך הכנסות, חיסכון נקי, או קטגוריה ספציפית.
  - גרף עמודות משווה המציג שינוי בערך מוחלט (₪) ובאחוזים (%).
  - טבלת Drill-down המציגה את הקטגוריות שחוו את העלייה/ירידה המשמעותית ביותר ("Top Movers").
- **תנאים לסיום המשימה (Definition of Done)**:
  1. בחירת שתי תקופות מציגה השוואה מלאה תוך פחות מ-200ms.
  2. זיהוי והדגשת 5 הקטגוריות שהשפיעו ביותר על הפער בין התקופות.
  3. תמיכה בייצוא תצוגת ההשוואה לדוח מסכם.

---

#### 📌 TASK-29: חישוב שיעור חיסכון חודשי וקצב שריפת מזומן (Savings Rate & Burn Rate)
- **עדיפות**: גבוהה
- **מורכבות**: בינונית
- **ספרינט**: Sprint 12
- **שכבות**: `features/insights_analytics/` (Savings Metrics)
- **ישויות קשורות**: `Transaction`, `PeriodicSummary`, `SavingsGoal`
- **מסכים רלוונטיים**: מסך 19 (ניתוח מגמות והשוואות)
- **סעיף ב-SDD**: 4.24
- **תיאור המשימה**:
  - חישוב מדדי מפתח פיננסיים:
    $$\text{שיעור חיסכון} = \frac{\text{הכנסות} - \text{הוצאות}}{\text{הכנסות}} \times 100$$
  - חישוב ממוצע נע של שיעור החיסכון ל-3, 6 ו-12 חודשים.
  - קצב צריכת מזומן חודשי (Burn Rate) מנוקה מתנועות חד-פעמיות חריגות ומהעברות פנימיות.
  - הגדרת יעד חיסכון אישי (למשל: 30% חיסכון חודשי) והצגת קו מטרה מעל גרף הביצועים בפועל.
- **תנאים לסיום המשימה (Definition of Done)**:
  1. חישוב מדויק של שיעור החיסכון והממוצעים הנעים.
  2. סימון תנועה כ"חריגה חד-פעמית" מוציא אותה מחישוב קצב שריפת המזומן הרגיל.
  3. גרף עמידה ביעד חיסכון חודשי עם אינדיקציה חזותית ברורה.

---

#### 📌 TASK-30: דשבורד ראשי מותאם אישית ותקציר פיננסי תקופתי
- **עדיפות**: קריטית
- **מורכבות**: גבוהה
- **ספרינט**: Sprint 13
- **שכבות**: `features/insights_analytics/` (Main Dashboard & Widgets)
- **ישויות קשורות**: `DashboardWidgetConfig`, `PeriodicSummary`
- **מסכים רלוונטיים**: מסך 1 (דשבורד ראשי)
- **סעיף ב-SDD**: 4.21, 6.2
- **תיאור המשימה**:
  - פיתוח מסך הפתיחה הראשי המרכז את כל המידע הקריטי ביותר בתצוגת ווידג'טים אינטראקטיביים:
    - יתרת חודש נוכחי (הכנסות מול הוצאות ומאזן).
    - תקציבים בסיכון חריגה.
    - חיובי כרטיסי אשראי קרובים.
    - שיעור חיסכון חודשי עדכני.
    - שינוי בשווי הנקי הכולל.
  - מחולל תקציר תקופתי אוטומטי (Weekly/Monthly Brief) בשפה טבעית בעברית (למשל: "החודש הוצאת 12% פחות מהחודש הקודם").
  - אפשרות סידור מחדש (Reorderable Grid/List) של הווידג'טים בהתאמה אישית.
  - ממשק נקי, טיפוגרפיה ברורה, אייקונים אחידים ללא אימוג'ים.
- **תנאים לסיום המשימה (Definition of Done)**:
  1. טעינת הדשבורד בפחות מ-300ms בהפעלת האפליקציה.
  2. לחיצה על כל ווידג'ט מעבירה ישירות למסך המקור המפורט.
  3. סידור הווידג'טים נשמר בהגדרות המשתמש ב-DB המקומי.

---

### Phase 7: גיבוי, שחזור, אופטימיזציה, בדיקות והשקה

---

#### 📌 TASK-31: מנגנון גיבוי מלא, שחזור וייצוא נתונים (JSON / CSV / SQLite)
- **עדיפות**: קריטית
- **מורכבות**: גבוהה
- **ספרינט**: Sprint 14
- **שכבות**: `features/backup_settings/` (Backup & Restore Engine)
- **ישויות קשורות**: `BackupFile`, `ExportJob`
- **מסכים רלוונטיים**: מסך 22 (הגדרות גיבוי, שחזור וייצוא)
- **סעיף ב-SDD**: 4.27, נספח 7.4
- **תיאור המשימה**:
  - פיתוח מנגנון ייצוא מלא של כלל בסיס הנתונים:
    1. קובץ גיבוי מלא מוצפן/חתום (`.backup` / `.json`) הכולל את כל הישויות, הקשרים וההגדרות.
    2. ייצוא טבלאות שטוחות ל-CSV / Excel לצורך ניתוח אישי באקסל.
  - מנגנון שחזור (Restore) מלא של קובץ גיבוי באפליקציה חדשה:
    - אימות תקינות הקובץ באמצעות Checksum (SHA-256) לפני תחילת השחזור.
    - בדיקת שלמות נתונים ומניעת דריסה שגויה במקרה של קובץ פגום.
  - אפשרות לגיבוי אוטומטי תקופתי לתיקייה מקומית במכשיר.
- **תנאים לסיום המשימה (Definition of Done)**:
  1. ייצוא קובץ גיבוי ממכשיר א' ושחזורו המלא במכשיר ב' מחזיר בדיוק את כל הנתונים, החשבונות וההיסטוריה.
  2. ניסיון שחזור קובץ פגום/שגוי נחסם עם הודעת שגיאה ברורה ללא פגיעה בנתונים הקיימים.
  3. ייצוא ל-CSV מפיק קבצים תקינים בעברית (UTF-8 with BOM).

---

#### 📌 TASK-32: אופטימיזציית ביצועים ועיבוד רקע (Background Isolates)
- **עדיפות**: קריטית
- **מורכבות**: גבוהה
- **ספרינט**: Sprint 15
- **שכבות**: כלל השכבות
- **סעיף ב-SDD**: 6.1
- **תיאור המשימה**:
  - ביצוע פרופיילינג מקיף באמצעות Flutter DevTools (CPU & Memory Profiler).
  - העברת חישובים כבדים (ייבוא קובצי ענק, אלגוריתם Deduplication, חישוב לוחות סילוקין וניתוח מגמות רב-שנתי) ל-Background Isolates (`compute` / `Isolate.spawn`) למניעת תקיעות ב-UI.
  - אופטימיזציית שאילתות Drift, שימוש נכון ב-Indexes וטעינה עצלה (Lazy Loading / Virtual Pagination) לרשימות גדולות.
  - וידוא עמידה בדרישת ה-SDD: טעינת מסך תנועות עם 10,000 רשומות בפחות משנייה אחת.
- **תנאים לסיום המשימה (Definition of Done)**:
  1. טעינת 10,000 תנועות במסך התנועות מתבצעת בפחות מ-800ms.
  2. ייבוא קובץ של 1,000 שורות אינו גורם ל-Jank או להקפאת האנימציות ב-UI.
  3. זיכרון RAM יציב ללא Memory Leaks לאורך שימוש ממושך.

---

#### 📌 TASK-33: בדיקות אינטגרציה, כיסוי Unit Tests והכנה ל-Release
- **עדיפות**: קריטית
- **מורכבות**: בינונית-גבוהה
- **ספרינט**: Sprint 15-16
- **שכבות**: `test/`, `integration_test/`
- **סעיף ב-SDD**: 2.1, פרק 6
- **תיאור המשימה**:
  - כתיבת בדיקות יחידה (Unit Tests) לכל ה-Use Cases והחישובים הפיננסיים (כיסוי של לפחות 80% ללוגיקה העסקית ב-Domain).
  - כתיבת בדיקות אינטגרציה (Integration Tests) לזרימות המרכזיות:
    - הזנת עסקה מהירה -> בדיקת עדכון יתרת חשבון ותקציב.
    - ייבוא קובץ אשראי -> זיהוי כפילויות -> סיווג אוטומטי -> אישור.
    - נעילה ביומטרית ושחזור באמצעות קוד PIN.
  - בדיקות תאימות ורספונסיביות מלאות על מכשירי אנדרואיד אמיתיים, טאבלט וסביבת דסקטופ (Windows).
  - הגדרת Build Configurations ל-Release (חתימת אפליקציה, ProGuard/R8, מיניפיקציה).
- **תנאים לסיום המשימה (Definition of Done)**:
  1. כל בדיקות היחידה והאינטגרציה עוברות בהצלחה (100% Pass Rate ב-CI).
  2. נוצר קובץ APK/AAB לאנדרואיד וקובץ Exe/MSIX לווינדוס הנפתחים ורצים בצורה חלקה.
  3. אין אזהרות לינטר או חריגות מה-SDD.

---

## 5. מטריצת מעקב מסכים מול משימות (Screen Coverage Matrix)

| # | שם המסך באפיון | משימות פיתוח רלוונטיות | שכבת Feature ראשית |
|---|---|---|---|
| 1 | דשבורד ראשי | TASK-26, TASK-30 | `features/insights_analytics/` |
| 2 | הזנה מהירה (Modal / Bottom Sheet) | TASK-07, TASK-08, TASK-10 | `features/transactions/` |
| 3 | רשימת תנועות וסינון מתקדם | TASK-12 | `features/transactions/` |
| 4 | פרטי תנועה / עריכה | TASK-07, TASK-08, TASK-10, TASK-12 | `features/transactions/` |
| 5 | רשימה וניהול הוראות קבע | TASK-11 | `features/transactions/` |
| 6 | העברה בין חשבונות | TASK-09 | `features/transactions/` |
| 7 | רשימה ופירוט חשבונות ואמצעי תשלום | TASK-06 | `features/accounts/` |
| 8 | ייבוא מקובץ (העלאה, מיפוי, תצוגה מקדימה, אישור) | TASK-13, TASK-14 | `features/import_export/` |
| 9 | עסקאות לסיווג (הצעות אוטומטיות) | TASK-15 | `features/auto_categorization/` |
| 10 | לוח חיובי כרטיסי אשראי | TASK-19 | `features/cash_flow/` |
| 11 | תחזית תזרים מזומנים | TASK-20 | `features/cash_flow/` |
| 12 | תכנון תקציב חודשי | TASK-16, TASK-17 | `features/budgets/` |
| 13 | מעקב ניצול תקציב | TASK-17, TASK-18 | `features/budgets/` |
| 14 | תיק השקעות - רשימת אחזקות | TASK-22 | `features/investments/` |
| 15 | פרטי אחזקה / עסקת השקעה | TASK-22, TASK-25 | `features/investments/` |
| 16 | נכסים פנסיוניים | TASK-23 | `features/pension_assets/` |
| 17 | נכסים לא סחירים והתחייבויות | TASK-24 | `features/non_market_assets/` |
| 18 | שווי נקי כולל | TASK-26 | `features/net_worth/` |
| 19 | ניתוח מגמות והשוואות | TASK-27, TASK-28, TASK-29 | `features/insights_analytics/` |
| 20 | הגדרות - עץ קטגוריות, תגיות ובתי עסק | TASK-05 | `features/categories_tags/` |
| 21 | הגדרות - אבטחה ונעילה | TASK-02 | `features/auth_lock/` |
| 22 | הגדרות - גיבוי, שחזור וייצוא | TASK-31 | `features/backup_settings/` |
| 23 | מסך כניסה / נעילה ביומטרית | TASK-02 | `features/auth_lock/` |

---

## 6. מטריצת דרישות לא-פונקציונליות ואימותן

| דרישה לא-פונקציונלית | יעד מוגדר באפיון (SDD) | משימות המבטיחות עמידה | שיטת אימות |
|---|---|---|---|
| **ביצועים (Performance)** | טעינת 10,000 תנועות בפחות משנייה; הזנה ב-Optimistic UI | TASK-03, TASK-07, TASK-12, TASK-32 | בדיקות עומס אוטומטיות ומדידת Frame Rendering ב-DevTools |
| **שפה וכיווניות (RTL)** | ממשק עברי מלא בכל הרכיבים, טבלאות, דיאלוגים וגרפים | TASK-01, TASK-04 | בדיקה ויזואלית בכל המסכים ו-RTL Layout Inspector |
| **אייקונוגרפיה (No Emojis)** | ללא אימוג'ים כלל; מבוסס אייקונים וצבע בלבד | TASK-01, TASK-05, TASK-30 | סריקה סטטית של הקוד (Custom Linter Rule) |
| **עיצוב חזותי (Theme)** | ערכת נושא בהירה בלבד (Light Theme) עם היררכיה ברורה | TASK-01, TASK-04 | בדיקות UI ו-Golden Tests |
| **זמינות Offline** | פונקציונליות מלאה ללא רשת; שערים עם תאריך עדכון | TASK-03, TASK-21 | הרצת תרחישי בדיקה במצב טיסה (Airplane Mode) |
| **אבטחה ופרטיות** | הצפנה במנוחה (SQLCipher), ביומטרי, Keystore, 0 דליפת נתונים | TASK-02, TASK-03, TASK-31 | ביקורת אבטחה (Security Audit) וסריקת תעבורת רשת |
| **רספונסיביות** | התאמה אוטומטית לטלפון, טאבלט ודסקטופ (Breakpoints) | TASK-04, TASK-33 | בדיקת ממשק ברזולוציות מגוונות (360dp, 768dp, 1200dp) |

---
**סוף מסמך פירוק משימות לפיתוח - נבנה ואומת מול SDD_Financial_tracking.md**
